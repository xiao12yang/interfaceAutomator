- baseInfo:
    api_name: 获取下单物料信息
    url: /api/order/customer/orderPlan/getMaterial
    method: get
    headers: ${get_headers(json)}
    cookies: ${get_extract_data(cookies)}
  testCases:
    - case_name: 正常获取下单无聊信息校验
      validation:
        - eq: {'message':'操作成功'}
      extract_list:
        materIds: $.material[*]
